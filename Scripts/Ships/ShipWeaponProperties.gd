extends Resource
class_name ShipWeaponProperties

# Holds the characteristics of a ships weapon type and position

@export var hardpoint: Vector3
@export var selected: bool = true
@export var weapon_type: int